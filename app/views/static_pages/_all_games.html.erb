<h3>You are involved in <%= pluralize(@total_games.count, 'game') %>.</h3>
You challenged other people to <%= pluralize(@games_as_challenger.count, 'game') %>.<br>
<% @games_as_challenger.each do |g| %>
    <%= link_to "#{g.challenger.email} vs #{g.challenged.email}", board_path(g.id) %><br>
<% end %>
<br><br>

Other people challenged you to <%= pluralize(@games_as_challenged.count, 'game') %>.<br>
<% @games_as_challenged.each do |g| %>
  <%= link_to "#{g.challenger.email} vs #{g.challenged.email}", board_path(g.id) %><br>
<% end %>